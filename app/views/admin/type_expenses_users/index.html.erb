<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Type expenses users</h1>
    <%= link_to "New type expenses user", new_admin_type_expenses_user_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <div id="type_expenses_users" class="min-w-full">
    <%= render @type_expenses_users %>
  </div>
</div>
